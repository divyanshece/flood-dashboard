# Backend Environment Variables

# Server port
PORT=3001

# Frontend URL for CORS (your Vercel domain)
FRONTEND_URL=https://your-app.vercel.app

# SQLite database path (for Render, use the disk mount path)
SQLITE_DB_PATH=/data/flood_hyderabad.db

# Environment
NODE_ENV=production
